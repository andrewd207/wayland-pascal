<?xml version="1.0" encoding="UTF-8"?>
<project>
  <name>wayland-server</name>
  <!-- version inherited from aggregator -->
  <author>Andrew Haines</author>
  <license>BSD-3-Clause</license>

  <!-- Aggregator for the server-side stack. For now just the hand-written
       runtime base (rt); the generated server protocol tiers
       (wayland-server-stable / -unstable / -staging) come once the generator
       grows a server mode. Like the client stack it builds on the shared
       wayland-common transport. -->
  <build>
    <packaging>pom</packaging>
  </build>

  <modules>
    <module>rt</module>
  </modules>
</project>
